func fib(Int: first)
{
  If(first < 2) return 1;
  Int b = fib(first-1) + fib(first-2);
  return b;
}

func main()
{
   Int aa = fib(0);
   Int ab = fib(1);
   Int ac = fib(2);
   Int ad = fib(3);
   Int ae = fib(4);
   Int af = fib(5);
}